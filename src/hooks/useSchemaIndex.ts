/**
 * useSchemaIndex Hook — re-export shim
 *
 * Canonical location: src/lib/schema/useSchemaIndex.ts
 * This file re-exports for backwards compatibility with existing imports.
 */

export { useSchemaIndex } from '@/lib/schema/useSchemaIndex'
